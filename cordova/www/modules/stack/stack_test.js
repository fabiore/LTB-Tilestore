'use strict';

describe('LTBApp.stack module', function() {

  beforeEach(module('LTBApp.stack'));

  describe('stack controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var stackctr = $controller('StackController');
      expect(stackctr).toBeDefined();
    }));

  });
});